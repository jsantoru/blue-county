# Verification — September 21, 2026

The **driver and on-foot exploration pass passes 118 tests across 18 files** and the production build. New actual-Rapier regressions cover walking, running, analog diagonals, slopes, curb stepping, waist-high barriers, wall sliding, jump buffering/coyote time, repeated-jump prevention, floor contact, safe door exits, blocked-door fallback, and camera collision. Character geometry/seat alignment and scenery collider disposal are checked separately. A vehicle suspension regression confirms that pedestrian-only scenery cannot change tire support.

The complete first browser review passed 11 checks; the separate second review passed 19 after additional input, camera, UI and performance polish. Both exercised real keyboard controls; the second also exercised mouse capture, right-drag, synthetic controller actions, controller disconnect/reconnect, paused animation, repeated scene recreation, collision with the parked car, grounded creek walking, map-boundary handling and ten seconds of yielding traffic without recovery teleports. Ten final screenshots were visually reviewed, including seated, walking, running, creek and 1280×720 HUD views. Both reports record zero runtime, resource or WebGL errors; the existing nonfatal ANGLE precision warning remains. Physical-controller feel and rumble remain unverified. See [exploration notes](exploration-pass.md), [first review](exploration-first-verification.json) and [second review](exploration-second-verification.json).

The final independent moving on-foot sample at 1920×1080 High covered 29.94m over 12.0112 seconds: 674 frame intervals, **18.0ms median, 18.1ms p95, 18.5ms maximum**, with approximately 1ms physics time. An earlier stationary sample after quality changes was slower (33.3ms median, 66.7ms p95); the final pass uses a warmup and actual movement, so these are observations rather than a controlled benchmark comparison or a universal frame-rate guarantee.

The existing full `npm run test:browser` also passes on the final exploration build: controller settings and input safety, all 777 ordered gates across three actual-physics laps in **6:35.60**, results restart and Return Home, with zero browser errors. See [browser-verification.json](browser-verification.json). The sections below retain historical results for earlier map and vehicle milestones.

The **Home backyard / Stony Creek pass passes 80 tests across 15 files** and the production build. Five backyard regressions were repeated after the final forest-floor material refinement. Tests compare the compiled stream with the untouched USGS coordinates, preserve the observed lawn/woods boundary, verify that water descends above its channel bed, and check that every existing road, driveway and house grade remains unchanged within 0.00001m. Regional terrain raycasts cover the carved floor, all four patch seams, corners and the unchanged surrounding terrain. Vegetation and renderer resource checks retain their coverage.

`npm run test:backyard` passes on the final production build. Five views were visually inspected from the deck, lawn edge, creek bank, creek bend and above the woods. The report records 184 stations over 212.26m, 226 representative mature woodland trees and 969 understory plants. Its actual Home departure stays grounded across 556 physics steps; quality changes and handling-grounds → Home recreation preserve the scene. A 12.015-second moving sample at 1920×1080 High measured 18.0ms median and 18.1ms p95 frame intervals on the GTX 1660 SUPER. There were no runtime, resource or WebGL errors. The existing nonfatal ANGLE precision warning is retained. See [backyard-verification.json](backyard-verification.json), [rendered views and limits](backyard-pass.md), and the [source aerial comparison](research-backyard/woodland-review.png).

The broader `npm run test:property` also passes on the backyard build, including all 463 departure steps with no pavement excursion, airborne step or building overlap, and the full connected 1.219km Beverly loop with no airborne batches. Parked-car restraint/release, graphics/resizing and Home recreation remain functional. Twelve neighborhood screenshots were refreshed. Its independent 12-second 1080p High sample measured 18.0ms median and 18.1ms p95, with no runtime, resource or WebGL errors. See the current [property report](property-verification.json); measurements below describe earlier builds.

The **Home photo detail pass passes 69 tests across 12 files** and the production build. Four relevant suites (14 tests) were repeated after the camera integration. New geometry regressions check recessed openings, the reflected house frame and roof winding, the asymmetric facade, aligned side doors, a nonintersecting pedestrian walkway and its full-width overlap with the existing driveway apron. The house and driveway source footprints remain unchanged.

`npm run test:home` passes on the production build. Its five inspected screenshots cover the actual starting chase camera, front facade and lawn, close front details, driveway-side deck/patio and rear screened area. The check confirms the starting camera stays outside the deck, then drives Home's actual departure over 556 fixed physics steps with no airborne steps. High/Low and handling-grounds → Home rebuilds retain the house, yard and tree metadata. There are no runtime, resource or WebGL errors; the report retains the existing nonfatal ANGLE numeric-precision warning. See [Home detail notes](home-detail-pass.md) and [home-verification.json](home-verification.json). Photographic comparison is a visual review, not a measured survey of hidden construction or garden grades.

The broader `npm run test:property` also passes on this Home build: 463 departure steps with no pavement excursion, airborne step or house overlap, the full 1.219km neighborhood loop, parked-car restraint/release, graphics/resizing and Home recreation. Its 12.01-second 1080p High sample measured 16.7ms median and 16.8ms p95 on the GTX 1660 SUPER, with no runtime, resource or WebGL errors. The updated [property report](property-verification.json) counts 11 generic decks plus the custom Home deck, and three generic patios plus Home's patio; the source aerial inventory is retained separately.

The preceding **Beverly property pass passed 62 tests across ten files** and the production build. Its regressions cover concave pavement following exact terrain triangles, the full parked car and connected Home departure, property vegetation exclusions, sign-face readability, deck geometry, and number 28's actual two-wing colliders leaving its terrace open. Source audits independently check all 62 source property polygons against 453 rendered building envelopes with zero overlaps. The original NYS building polygons and road topology remain unchanged.

`npm run test:property` checks the Home parking restraint and release, all 463 physics steps of its departure, the full neighborhood loop, graphics presets/resizing and handling-grounds → Home recreation. Thirty simulated idle seconds move the parked car less than 0.00001m horizontally, with four grounded wheels; explicit throttle and recovery both restore motion. The car leaves Home without a pavement excursion, airborne step or house overlap. Actual renderer metadata confirms number 28's two wings share their floor/roof base and appearance without a duplicate entrance. Screenshots and measured browser results are in [property-pass.md](property-pass.md) and [property-verification.json](property-verification.json). Source confidence and vertical-grade limitations are documented separately from the geometry checks.

The final 12-second 1080p High moving sample on the GTX 1660 SUPER measured 16.7ms median and 16.8ms 95th-percentile frame intervals. Earlier samples of the same property scene reached 33.4ms at the 95th percentile, so this is not a stable-60fps guarantee. There were no runtime, resource or WebGL errors; one nonfatal ANGLE shader numeric-precision warning is retained in the report. Property geometry adds 21 material batches and 22,027 triangles.

The final `npm run test:browser` also passes on this build: synthetic-pad settings, proportional triggers, disconnect/focus safety, a complete actual-physics three-lap race through 777 gates in **6:35.58**, results restart and Return Home. It records zero browser errors in [browser-verification.json](browser-verification.json). No physical-controller feel or rumble is claimed.

The earlier **Beverly detail pass passed 52 tests across eight files**, the production build, and `npm run test:detail`. Its geometry regressions verify real openings behind recessed glass and measured crown radii through varied tree shapes at both detail levels. Its historical measurements and images remain in [detail-pass.md](detail-pass.md) and [beverly-detail-verification.json](beverly-detail-verification.json). The reports below document earlier milestones.

The first Beverly reconstruction built successfully with TypeScript, Vite 8.3, Three.js 0.186 and Rapier 0.20. Dependency versions are locked in `package-lock.json`. **50 tests across seven files passed**, and the full browser race/restart/Home flow was repeated successfully in Edge 153.0.4234.48 with zero page errors and a **6:35.58** race result. The earlier build was also checked in Chrome 152.0.7977.83; its separate report remains `browser-verification-chrome.json`.

The Beverly-specific browser check completes the real **1,218.89m neighborhood loop** using actual vehicle physics without teleporting or losing the driving surface, exercises High/Medium/Low graphics, and captures the rebuilt neighborhood. A separate 12-second moving sample at 1920×1080 High measured **16.70ms median / 16.80ms 95th-percentile** frame intervals on the GTX 1660 SUPER. See [beverly-reconstruction.md](beverly-reconstruction.md) for evidence and limits, and [beverly-verification.json](beverly-verification.json) for the full report. The eight new automated tests verify reference geometry, loop topology, facade evidence, driveway occlusion, vegetation exclusions, bay-window orientation, entrance access and concrete materials.

The earlier neighborhood visual upgrade passed all 42 tests, the production build, all three graphics presets and the full browser race/restart/Home flow. Its final race time was 6:35.62. Its 1080p High moving sample measured a 16.70 ms median and 16.80 ms 95th-percentile frame interval on the GTX 1660 SUPER. See `visual-upgrade.md` for the measured scope and `visual-verification.json` for the full report.

The corrected GLB was visually checked in the actual game: steering wheel, instrument cluster and pedals are on the driver's left. Seven export assertions check interior handedness, and wheel names are resolved from geometry rather than historical object labels. New actual-physics regressions check manual right/left steering and civilian right-lane tracking at four headings. Right-stick look direction, oncoming-lane credit and the Home shoulder were aligned with the corrected axis convention. The active source `.blend` and saved snapshot retain their original SHA-256. Screenshot: `left-hand-drive-game.png`.

## What was exercised

- Input unit tests: nonzero device selection, fresh snapshots/null entries, simultaneous proportional triggers, deadzone rescaling, menu edges/repeat, saved settings, unknown mappings, remapping, confirmation consumption, View hold, focus/disconnect latches, processed diagnostics and haptic scheduling/rejection.
- Actual Rapier scenarios: acceleration, partial/full throttle, braking to a stop without accidental reverse, ordinary turning and boosted wall collision at 30/60/120Hz rendered-frame schedules with fixed60 physics. Tolerances: 0.15m position, 0.2m/s speed, 0.1 boost units. No promise of deterministic results across hardware.
- Separate CCD tests initialize the chassis at the configured 70m/s boost ceiling and confirm it cannot tunnel through a thin fixed barrier or a stationary physical traffic car. The traffic car receives collision momentum. These are explicit high-speed initial conditions, not measured acceleration runs.
- Race rules: ordered directional gates, wrong-way rejection, out-of-order rejection, reset/teleport blocking, lap completion, reserve bounds/cooldowns and takedown deduplication.
- Map/surface tests: terrain diagonal agreement and road surface alignment. Offline importer asserts anchor distance, source-graph continuity, loop closure, map bounds and <=12m road samples. Rebuilding cached map data twice produced identical output.
- Full route with actual physics: all four racers completed all777 ordered gate crossings across3laps with3 physical civilian cars. No racer recovery/teleport was needed. Completion times in the controlled Node test:395.75s,556.32s,403.73s,396.23s. The slow rival was delayed by interactions; the simulation does not award artificial progress. See `vehicle-route-physics-traffic.json`.
- Browser race: real442 GLB loaded and rendered, countdown completed, actual physics driver completed3laps, latest Beverly build results reported6:35.58. Synthetic pad then restarted from lap1/checkpoint1, and Return Home exited the event. See `browser-verification.json` and `browser-race-results.png`.
- Browser Home exit: actual physics drove from the roadside Home spawn, followed the eastern portion of Beverly Drive, crossed the verified junction, and reached West Ridge Road at local[128.540,10.786,40.715], four grounded wheels and0.005m centerline error. No teleport along the route.
- Boundary recovery: an explicit test placement beyond the eastern edge triggered safe recovery to a mapped road at local[1846.038,24.830,38.620], approximately0.009m from its centerline. Free Drive stayed outside race mode. Teleport injection was used only to set up this boundary test.
- Synthetic pad integration: standard-mapped index2 with null slots before it; partial LT/RT together; menus/settings slider; acceleration; disconnect while accelerating; reconnect selection followed by deliberate resume; blur/focus followed by deliberate resume; race/results/restart/Home. Injection is confined to the test script and `?test` debug hooks.
- Visual browser inspection: source car appearance, closed hood, separate steer/spin nodes, map road continuity, Home clearance, slopes, race result screen, and route preview. Shader compiled successfully for pooled smoke. The test-area drift sample reached20.95m/s with four grounded wheels and21.3° slip; this is functional evidence, not a physical feel judgment.

## Baseline performance before the neighborhood visual upgrade

Measured in the actual Windows browser at1920×1080, pixel ratio1, **High** graphics, shadows on, using the production build and real442 with all six other cars active. Hardware: AMD Ryzen5 3600, NVIDIA GeForce GTX1660 SUPER, ANGLE Direct3D11. Browser: Edge153.0.4234.48. A scripted driver followed the first part of the race in real time, with fixed60 physics and interpolated rendering.

Independent raw `requestAnimationFrame` timestamps over a continuous20s sample, excluding the first10 warm-up intervals:

| Measure | Observed |
|---|---:|
| Measured intervals | 1,185 |
| Median | 16.70ms |
| 95th percentile | 16.80ms |
| 99th percentile | 16.80ms |
| Maximum | 16.90ms |
| Mean | 16.68ms (about60fps) |
| Last measured simulation step | 0.80ms |
| Last rendered draw calls | 171 |
| Last rendered triangles incl. shadow passes | 976,679 |

These measurements cover a short moving race sample, not every map location or a long thermal soak. They do not measure display latency or physical controller latency. High uses2048px shadows at native CSS resolution; Medium uses1024px shadows; Low uses75% render resolution and disables shadows. The renderer batches static scenery by material and pools effects.

## Hardware checks still pending

No actual Gamepad device was exposed in the browser during verification. Wired USB Xbox feel, button layout on your specific controller, end-to-end latency and felt rumble remain unverified. Complete the short checklist in `controller.md`. Haptic feature detection/rejection was tested synthetically, and vibration is optional.

The neighborhood uses real topology and public elevation but approximate building envelopes/materials/vegetation. It is not a precise reconstruction of your dad’s house. Road widths and racing presentation are explicit gameplay adjustments in the geographic manifest. The newer visual build and its measured frame intervals are documented separately in `visual-upgrade.md` and `visual-verification.json`; the performance table above belongs to the original flat-color build.

## Reproduce

Run `npm test`, then `npm run build`. Serve the production build with `npm run preview -- --port 5180 --strictPort`. In another PowerShell terminal set `$env:GAME_URL='http://127.0.0.1:5180'` and run `npm run test:browser` and `npm run test:property`. The property check uses installed Edge; the general browser test also supports a Playwright browser selected through `BROWSER_CHANNEL`. The `?test` URL enables explicit developer hooks; normal play exposes no input-injection API.
